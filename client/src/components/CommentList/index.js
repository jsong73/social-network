import React from "react";

const CommentList = ({ comments = [] }) => {
    if(!comments.length) {
        return "No comments to view as of yet!"
    }

    return(
        <div>
            
        </div>
    )
}